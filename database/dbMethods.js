const { Title } = require('./index');
const { formatTitleObj } = require('./objFormatting.js');


// Insert database title record
var insertTitle = (obj) => {

  var formattedObj = formatTitleObj(obj);
  //insert document

}
